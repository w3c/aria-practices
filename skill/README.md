# Cursor Skill: WAI-ARIA APG

Distributable skill for Cursor. When enabled, the AI follows WAI-ARIA Authoring Practices when implementing accessible components.

**Copy to your project:**

```sh
cp -r skill/wai-aria-apg .cursor/skills/
```

Requires the APG MCP server in Cursor settings. See the main README for MCP setup.
